<%= render partial: "specialty_buttons" %>

<div class="row">
  <div class="col s1"><br></div>
  <div class="col s3">
    <%= form_for(:filter, url: filter_path) do |f| %>
    <%= select_tag "Sort By", options_for_select(["Rate: Ascending", "Rate: Descending"]), id: "sort-by"%>

    <ul>
      <% if @tags %>
        <% @tags.each do |tag| %>
            <li>
              <%= check_box_tag(tag.name) %>
              <%= label_tag(tag.name, tag.name) %>
            </li>
        <% end %>
      <% end %>
    </ul>
    <%= f.submit "Apply Sort", id: "filter" %>

    <% end %>
  </div>
</div>

<div class="row">
  <%= render @developers %>
</div>
