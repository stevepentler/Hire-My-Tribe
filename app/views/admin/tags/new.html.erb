<%= form_for [:admin, @tag] do |f| %>
  <%= f.label :name %> 
  <%= f.text_field :name, id: "form-value" %> 

  <%= f.submit "Submit Tag", class: "form-label left-button", id: "form-button" %> %> 
<% end %> 